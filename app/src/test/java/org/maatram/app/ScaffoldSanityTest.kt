package org.maatram.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder so `testDebugUnitTest` has something to run on the empty
 * scaffold. Replaced by real ViewModel/repository tests as screens land.
 */
class ScaffoldSanityTest {
    @Test
    fun `unit test wiring works`() {
        assertEquals(4, 2 + 2)
    }
}
